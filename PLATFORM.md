# JDKS ARENA — แผนพัฒนาเป็นแพลตฟอร์ม (Multi-tenant)

เป้าหมาย: ครูคนไหนก็สมัครใช้ระบบนี้ได้เอง มีชุดคำถามของตัวเอง ใช้ชุดสำเร็จรูปได้
ปรับแบรนด์โรงเรียนได้ และแชร์ชุดคำถามให้ครูคนอื่นได้

---

## 1. สิ่งที่เปลี่ยนจากระบบเดิม

| ส่วน | เดิม (single-tenant) | ใหม่ (platform) |
|---|---|---|
| เนื้อหา | `settings/questions` ชุดเดียวทั้งระบบ | `packs/{packId}` — ครูมีได้หลายชุด |
| บัญชีครู | สร้าง `staff/{uid}` ด้วยมือใน Console | สมัครเองได้ → `teachers/{uid}` |
| เซสชัน | ใช้เนื้อหากลาง | ผูกกับ `packId` ที่เลือกตอนสร้าง |
| แบรนด์ | ฮาร์ดโค้ด "DIGITAL SURVIVAL" | ต่อครู + snapshot ลงเซสชัน |
| สิทธิ์ | staff/admin (ทุกคนเท่ากัน) | แยกตามเจ้าของข้อมูล (ownerUid) |

---

## 2. โครงสร้างข้อมูล (Firestore)

```
teachers/{uid}                  โปรไฟล์ครู (สร้างอัตโนมัติตอนสมัคร)
  { email, displayName, org, brand:{appName,accent,logoUrl}, createdAt }

packs/{packId}                  ชุดคำถาม (เนื้อหาทั้งชุดอยู่ในเอกสารเดียว)
  { ownerUid, title, subject, description,
    visibility: 'private' | 'public',
    missions:[...], levels:[...], badges:[...], xpRules:{...},
    questions:{ qid: {...} },   // ~34KB ต่อชุด — ต่ำกว่าลิมิต 1MB มาก
    questionCount, forkedFrom, createdAt, updatedAt }

sessions/{sessionId}            + packId, hostUid, brand (snapshot)
users/{sessionId}_{playerId}    นักเรียน + hostUid (denormalize)
answers/{sessionId}_{playerId}_{qid}   + hostUid
leaderboards/{sessionId}        + hostUid
stats/{sessionId}               + hostUid
staff/{uid}                     ผู้ดูแลแพลตฟอร์ม (เจ้าของระบบเท่านั้น)
```

### ทำไมต้อง denormalize `hostUid` ลงเอกสารนักเรียน/คำตอบ
กฎเดิมใช้ `isStaff()` = ครูคนไหนก็เขียนได้ทุกเซสชัน — พอมีหลายครูจะไม่ปลอดภัย
ถ้าให้กฎไป `get()` เซสชันทุกครั้ง จะเปลืองการอ่านมาก (120 คน × 23 ข้อ)
จึงเก็บ `hostUid` ไว้ในเอกสารตอน **สร้าง** (ตรวจครั้งเดียวด้วย `get()`)
แล้วการอ่าน/เขียนหลังจากนั้นเทียบตรงๆ ได้เลย — ปลอดภัยและถูก

---

## 3. กฎความปลอดภัย (หลักการ)

- `teachers/{uid}` — อ่าน/เขียนได้เฉพาะเจ้าของ
- `packs/{id}` — อ่านได้ถ้าเป็นเจ้าของ **หรือ** `visibility == 'public'`; เขียนได้เฉพาะเจ้าของ
- `sessions/{id}` — สร้างได้ถ้าเป็นครู (`teachers/{uid}` มีอยู่) และ `hostUid == uid`;
  แก้/ลบได้เฉพาะเจ้าของเซสชัน
- `users` / `answers` — นักเรียนสร้างของตัวเอง (ตรวจ `hostUid` ตรงกับเซสชันด้วย `get()` ครั้งเดียว);
  ครูเจ้าของเซสชันอ่าน/ให้คะแนนได้ (เทียบ `hostUid` ตรงๆ)
- คำตอบยัง **แก้ไม่ได้** (immutable) เหมือนเดิม

---

## 4. ลำดับการพัฒนา (แต่ละเฟสใช้งานได้จริงก่อนไปต่อ)

- [x] **P1 — บัญชีครู**: สมัครเอง, `teachers/{uid}`, เลิกใช้ staff gating, rules ใหม่ *(รอเผยแพร่ rules — ดูข้อ 7)*
- [x] **P2 — ชุดคำถาม (packs)**: สร้าง/แก้/ลบชุด, ชุดสำเร็จรูป, เซสชันผูก packId
- [x] **P3 — คลังชุดคำถาม**: `community.html` เปิดสาธารณะ, ก๊อปไปแก้ต่อ (fork), เล่นเดี่ยวได้โดยไม่ต้องสมัคร
- [ ] **P4 — แบรนด์**: ชื่อระบบ/โลโก้/สีของแต่ละครู แสดงทุกจอ
- [ ] **P5 — ขัดเงา**: onboarding, โควตากันใช้เกิน, คู่มือฉบับแพลตฟอร์ม, admin แยกตามครู

---

## 5. เรื่องที่ต้องตัดสินใจระหว่างทาง

- **ค่าใช้จ่าย**: หลายครูใช้พร้อมกัน ต้องเปิด Firebase Blaze + ตั้งงบเตือน
- **โควตา**: จำกัดจำนวนชุด/เซสชันต่อครู กันคนใช้เกินจนบิลบาน
- **ความเป็นส่วนตัว**: ระบบยังไม่เก็บข้อมูลส่วนตัวนักเรียน (ชื่อเล่นเท่านั้น) — คงหลักการนี้ไว้
- **การย้ายข้อมูลเดิม**: เซสชันเก่ายังไม่มี `packId`/`hostUid` ในเอกสารลูก → ต้องมีสคริปต์ย้าย หรือปล่อยเป็นข้อมูลเก่าอ่านอย่างเดียว


---

## 6. แผนผังหน้าจอ (หลังรื้อเป็น JDKS Arena)

| หน้า | ใคร | ทำอะไร |
|---|---|---|
| `index.html` | ทุกคน | หน้าแรก · กรอกรหัสห้องเข้าเล่น · ทางเข้าครู |
| `community.html` | ทุกคน (ไม่ต้องสมัคร) | คลังเกมสาธารณะ · ค้นหา · กด "เล่นเลย" หรือ "คัดลอกไปแก้" |
| `play.html?pack=<id>` | ทุกคน (ไม่ต้องสมัคร) | **เล่นเดี่ยว** ตามจังหวะตัวเอง ไม่มีห้อง ไม่เขียนฐานข้อมูล · `?pack=demo` = ชุดตัวอย่างที่ฝังมากับเว็บ |
| `login.html` | ครู | **หน้าเข้าสู่ระบบ/สมัคร ของตัวเอง** (แทน modal เดิม) รับ `?mode=signup` และ `?next=` |
| `teacher.html` | ครู | แผงของฉัน: ชุดของฉัน · ห้องของฉัน · คลังสาธารณะ |
| `editor.html?pack=` | ครูเจ้าของชุด | แก้ภารกิจ + คำถามทั้ง 5 รูปแบบ |
| `host.html` | ครู | **แผงคุมเกมแบบคอกพิต** — เต็มจอ ไม่เลื่อนทั้งหน้า: รางภารกิจซ้าย · คำถามที่ครูอ่านตรงกลาง · ตัวเลขสด+กราฟคำตอบ+อันดับขวา · แถบคุม 5 ปุ่มล่าง (แถบบน-ล่างเป็นสีเข้มทั้งสองธีม เพื่อแยกตัวควบคุมออกจากเนื้อหาที่ครูกำลังอ่าน) |
| `presenter.html` | จอฉาย | โจทย์ + การ์ดคำตอบสี · เวลา · แท่นอันดับ |
| `student.html` | นักเรียนในห้อง | **บัตรผ่านนักสู้** (อวตาร · XP · แถบเลื่อนระดับ · ความคืบหน้า) + จอตอบคำถาม |

ทั้งสามจอใช้ภาษาภาพชุดเดียวกัน: **การ์ดคำตอบ 5 สี 5 รูปทรง** (▲ ◆ ● ■ ★) ปรากฏพร้อมกันบนจอนักเรียน
จอฉาย และกราฟคำตอบของครู — ครูจึงพูดได้ว่า "สามเหลี่ยมแดงเยอะสุด" แล้วทั้งห้องเข้าใจตรงกัน

โครงหน้าใช้ร่วมกันผ่าน `js/topbar.js` — วาง `<header class="jx-top" data-topbar>` แล้วมันจะเติมโลโก้ เมนู
และ **โซนบัญชี** (ยังไม่ล็อกอิน = ปุ่มเข้าสู่ระบบ/สมัครฟรี · ล็อกอินแล้ว = ชื่อครู + เมนูออกจากระบบ) ให้เอง

---

## 7. ⚠️ ค้างอยู่: ต้องเผยแพร่ Firestore Rules

ตรวจสอบกับฐานข้อมูลจริงแล้ว (15 ก.ย. 2569) พบว่ากฎที่ **เผยแพร่อยู่ตอนนี้ยังเป็นชุดเดิมของเวิร์กช็อป**:

| คอลเลกชัน | ผลทดสอบจริง |
|---|---|
| `sessions`, `settings`, `stats`, `leaderboards` | ✅ อ่านได้ |
| `teachers/{uid}` | ❌ `permission-denied` |
| `packs` (ทั้ง query และ get) | ❌ `permission-denied` |

นี่คือสาเหตุเดียวกับที่สมัครสมาชิกแล้วขึ้น `permission-denied` — บัญชีถูกสร้างใน Authentication แต่เขียน
`teachers/{uid}` ไม่ได้ ต้องเอาไฟล์ `firestore.rules` ในโปรเจกต์นี้ไปวางใน
**Firebase Console → Firestore Database → Rules → Publish** แล้วรอราว 1 นาที

หลังเผยแพร่แล้วจะใช้ได้: สมัครครู · สร้าง/บันทึกชุดคำถาม · เผยแพร่ลงคลังสาธารณะ · เปิดห้องสด

---

## 8. ระบบคะแนน XP (ปรับใหม่)

`js/scoring.js` เป็นแหล่งความจริงแหล่งเดียว ทั้งจอนักเรียน จอเล่นเดี่ยว และการให้คะแนนจริงฝั่งครู

```
XP = base × [ minFactor + (1 − minFactor) × (1 − เวลาที่ใช้ / เวลาที่ให้) ]
ค่าเริ่มต้น: base 1000 · minFactor 0.4 · ประเมินตนเอง 300 · จบภารกิจ +500
```

ตัวอย่างเมื่อให้เวลา 45 วินาที: ตอบทันที 1000 · 5 วิ 930 · 11 วิ 850 · 22 วิ 710 · 40 วิ 470 · เกือบหมดเวลา 400 · ตอบผิด 0

เลเวลถูกปรับตามสเกลใหม่: 0 / 3,000 / 7,000 / 12,000 / 18,000 XP


---

## 9. บันทึกวิธีตรวจงาน (สำคัญ)

`node --check ไฟล์.js` **ตรวจแบบ CommonJS** จึงปล่อยข้อผิดพลาดของ ES module ผ่านไปได้
(เคยปล่อย `const bar` ซ้ำในฟังก์ชันเดียวกันจนหน้าเว็บพังทั้งไฟล์) ให้ใช้แบบนี้แทนเสมอ:

```bash
for f in js/*.js; do node --input-type=module --check < "$f" || echo "FAIL $f"; done
```

และลำดับ CSS ของทุกหน้าต้องเป็น `style.css → arena.css → arena-game.css → <ไฟล์เฉพาะหน้า>.css`
เพราะ arena.css เขียนทับโทเคนและคอมโพเนนต์กลาง ถ้าไฟล์เฉพาะหน้ามาก่อน จะโดนทับหมด

**บทเรียนเพิ่ม:** ต้องทดสอบ **เส้นทางรีเฟรช** ไม่ใช่แค่เส้นทางเปิดครั้งแรก
— `host.html` เคยค้างที่ "กำลังเตรียมแผงควบคุม…" เพราะตอนรีเฟรชขณะมีห้องค้าง ระบบวาดแผงก่อนที่ชุดคำถามจะโหลด
(`state.content` ยังเป็น `null` → `TypeError` เงียบๆ) ตอนนี้ย้ายการวาดไปหลังสแนปช็อตแรก และมี watchdog 9 วินาที
พร้อมปุ่ม "เปิดห้องใหม่" ถ้าอ่านห้องไม่ได้


---

## 10. กติกาห้องเรียน (รอบล่าสุด)

**รหัสห้องใช้ได้ครั้งเดียว** — `createSession()` ตรวจว่ารหัสนั้นเคยมีอยู่ไหม (เปิดหรือปิดก็นับ) จึงไม่มีทางแจกซ้ำ
พอครูกด **จบกิจกรรม** ระบบจะปิดห้องแล้ว**ออกรหัสใหม่ให้อัตโนมัติ**ทันที (`endActivity()` → `rotateRoom()`)
นักเรียนที่กรอกรหัสเก่าจะเห็นข้อความว่า "ห้องนี้จบไปแล้ว — รหัสห้องใช้ได้ครั้งเดียว ขอรหัสใหม่จากครูได้เลย"

**นักเรียนกลับเข้าห้องได้เสมอ** — เดิมเป็นลูปปิดตาย: `openJoin()` เห็น `ds-player` ค้างอยู่ก็เด้งไป `student.html`
ทันที ส่วน `showNotJoined()` ไม่เคยล้างค่านั้น สองหน้าจึงโยนกันไปมาไม่จบ ตอนนี้
- `showNotJoined()` ล้าง `ds-player` ทิ้ง เพราะเอกสารผู้เล่นหายแล้ว = ข้อมูลค้างใช้ไม่ได้
- `openJoin()` ถามก่อนด้วยกล่องของเราเอง ("อยู่ห้อง X อยู่ — ออกแล้วเข้าห้องใหม่ไหม?") ไม่ตัดสินใจแทน
- รหัสผู้เล่นขยายเป็น 4 หลัก (P0000–P9999) โอกาสชนกันตอนเข้าใหม่เหลือ ~1% ที่ 120 คน

**คลังสาธารณะ = หยิบไปใช้ ไม่ใช่ก๊อป** — ครูคนอื่นกด "เปิดห้องให้ทั้งชั้น" แล้วเปิดห้องด้วยชุดต้นฉบับได้เลย
เจ้าของยังเป็นคนเดิม ไม่มีสำเนาให้แก้ (กฎ Firestore รองรับอยู่แล้ว: อ่าน pack สาธารณะได้ และสร้าง session ได้ถ้าเป็นครู)

---

## 11. ส่วนประกอบกลางที่เพิ่มเข้ามา

| ไฟล์ | หน้าที่ |
|---|---|
| `js/dialog.js` | `dxConfirm` / `dxAlert` / `dxPrompt` / `toast` — แทน `alert`/`confirm`/`prompt` ของเบราว์เซอร์ **ทั้งหมด 24 จุด** (เหลือ 0) |
| `js/icons.js` | ชุดไอคอนเส้น 54 ตัว — อิโมจิในหน้าแอปถูกแทนหมดแล้ว (เหลือเฉพาะ `guide.html` ที่เป็นเอกสารแยก) |
| `js/mascot.js` | ตัวการ์ตูน 3D 11 ตัว |
| `shapeIcon()` ใน `js/game.js` | รูปทรงการ์ดคำตอบเป็น SVG (เดิมใช้ ▲◆●■★ ซึ่งบางเครื่องเรนเดอร์เป็นอิโมจิสี) ใช้ร่วมกันทั้งจอนักเรียน จอฉาย และกราฟคำตอบของครู |

หน้าเอดิเตอร์: คำถามย้ายไป**อยู่ในการ์ดภารกิจของตัวเอง** (`createQuestionEditor({ only: missionId })`)
ไม่ต้องเลื่อนไปเลือกภารกิจจากการ์ดอื่นอีก

พื้นที่ทำงานขยายเต็มจอทุกขนาด (`.jx-wrap` / `.hx-main` ไม่มี `max-width` แล้ว) เหลือเพียงความกว้างอ่านสบาย
ที่บังคับไว้กับตัวเนื้อความเท่านั้น (`.jx-lead` 62ch)
